# Copy to .env.local for local Vercel dev (vercel dev).
# In production, set the same key via:  vercel env add MINIMAX_API_KEY
# Never commit .env.local — it's gitignored.

MINIMAX_API_KEY=sk-replace-with-your-minimax-key
